import SwiftUI

@main
struct Image_ConverterApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .defaultSize(width: 940, height: 680)
        .windowResizability(.contentMinSize)
    }
}
